SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
# only needed for scripts/migrate.mjs, never commit the real one
SANITY_WRITE_TOKEN=
# Vimeo API token (app "JOJX site"), scopes public + private + video_files.
# The site build uses it to resolve clip file URLs. Set it in Vercel too.
VIMEO_ACCESS_TOKEN=
